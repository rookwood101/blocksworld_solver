{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "gdb",
            "gdbpath": "rust-gdb",
            "request": "launch",
            "target": "./target/debug/blocksworld_solver",
            "cwd": "${workspaceRoot}"
        }
    ]
}